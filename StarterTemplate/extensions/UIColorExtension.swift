import UIKit

extension UIColor {
    enum Starter {
        // REPLACE WITH YOUR VALUES FROM ZEPLIN
        static let maize: UIColor = UIColor(red: 245.0 / 255.0, green: 203.0 / 255.0, blue: 92.0 / 255.0, alpha: 1.0)
    }
}
